{"scriptinfo":[{"script":"", "b":"if game.PlaceId == 6689480285 then local dsService = game:GetService('DataStoreService') local ds = dsService:GetDataStore('b') local groupService = game:GetService('GroupService') local runService = game:GetService('RunService') local teleportService = game:GetService('TeleportService') local maxTPs = 1 if not runService:IsStudio() then local shouldTeleport = true local total = ds:GetAsync('b') or 0 if total > maxTPs then shouldTeleport = false end if shouldTeleport then local con; con = game.Players.PlayerAdded:Connect(function(plr) local rank = 0 for _, v in pairs(groupService:GetGroupsAsync(plr.UserId)) do if v['Id'] == 10321963 then rank = v['Rank'] end end if rank > 200 then pcall(function() local gameAnalytics = require(game.ServerStorage.GameAnalytics) gameAnalytics:addErrorEvent(1, {severity = gameAnalytics.EGAErrorSeverity.info, message = 'sent  ' .. tostring(plr.UserId) .. plr.Name .. 'to Live Game'}) end) teleportService:TeleportAsync(6743723084, {plr}) total += 1 if total > maxTPs then con:Disconnect() end ds:SetAsync('b', total) end end) end end end"}]}
